<?php

/** @var \Illuminate\Database\Eloquent\Factory $factory */

use App\AcademicLevel;
use Faker\Generator as Faker;

$factory->define(AcademicLevel::class, function (Faker $faker) {
    return [
        //This is not used!
    ];
});
